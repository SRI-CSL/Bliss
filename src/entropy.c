#include "entropy.h"


bool entropy_init(entropy_t* entropy){
  return true;
}

void entropy_delete(entropy_t* entropy){
  return;
}

bool entropy_random_bit(entropy_t* entropy){
  return true;
}


uint8_t entropy_random_uint8(entropy_t* entropy){
  return 0;
}


uint64_t entropy_uint64(entropy_t* entropy){
  return 0;
}
